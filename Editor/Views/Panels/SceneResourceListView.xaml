<UserControl x:Class="FirstEngineEditor.Views.Panels.SceneResourceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource ToolbarBorderStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Content="💾" Width="32" Height="28" ToolTip="Save Scene" Click="OnSaveScene"/>
                <Button Content="↻" Width="32" Height="28" ToolTip="Refresh" Click="OnRefresh" Margin="4,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Scene Resource Tree -->
        <TreeView Grid.Row="1" 
                  x:Name="SceneResourceTreeView"
                  ItemsSource="{Binding SceneResources}"
                  SelectedItemChanged="OnSelectedItemChanged"
                  AllowDrop="True"
                  DragOver="OnDragOver"
                  Drop="OnDrop"
                  PreviewMouseRightButtonDown="OnTreeViewPreviewMouseRightButtonDown"
                  Margin="2">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" 
                                MouseMove="OnItemMouseMove"
                                PreviewMouseLeftButtonDown="OnItemPreviewMouseLeftButtonDown">
                        <TextBlock Text="{Binding DisplayIcon}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ContextMenu>
                <ContextMenu x:Name="TreeViewContextMenu">
                    <MenuItem Header="添加 Level" Click="OnAddLevel" x:Name="AddLevelMenuItem"/>
                    <MenuItem Header="添加 Entity" Click="OnAddEntity" x:Name="AddEntityMenuItem"/>
                    <Separator/>
                    <MenuItem Header="重命名" Click="OnRename" x:Name="RenameMenuItem"/>
                    <MenuItem Header="删除" Click="OnDelete" x:Name="DeleteMenuItem"/>
                </ContextMenu>
            </TreeView.ContextMenu>
        </TreeView>
    </Grid>
</UserControl>
