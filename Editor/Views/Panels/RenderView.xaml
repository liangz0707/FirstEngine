<UserControl x:Class="FirstEngineEditor.Views.Panels.RenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource ToolbarBorderStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Content="▶" Width="36" Height="28" Click="OnStartRender"/>
                <Button Content="⏸" Width="36" Height="28" Click="OnPauseRender" Margin="4,0,0,0"/>
                <Button Content="⏹" Width="36" Height="28" Click="OnStopRender" Margin="4,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Render Surface -->
        <Border Grid.Row="1" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1"
                Background="Black"
                Margin="2"
                Name="RenderSurface"
                AllowDrop="True"
                DragOver="OnRenderSurfaceDragOver"
                Drop="OnRenderSurfaceDrop">
            <Grid Name="RenderHostContainer"
                  Loaded="OnRenderHostLoaded"
                  SizeChanged="OnRenderHostSizeChanged">
                <!-- RenderViewportHost will be added here programmatically -->
            </Grid>
        </Border>
    </Grid>
</UserControl>
