<UserControl x:Class="FirstEngineEditor.Views.Panels.RenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="DarkGray" Margin="5">
            <Button Content="▶" Width="40" Click="OnStartRender"/>
            <Button Content="⏸" Width="40" Click="OnPauseRender" Margin="5,0,0,0"/>
            <Button Content="⏹" Width="40" Click="OnStopRender" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Render Surface -->
        <Border Grid.Row="1" 
                BorderBrush="Gray" 
                BorderThickness="1"
                Background="Black"
                Margin="5"
                Name="RenderSurface">
            <Grid Name="RenderHostContainer"
                  Loaded="OnRenderHostLoaded"
                  SizeChanged="OnRenderHostSizeChanged">
                <!-- RenderViewportHost will be added here programmatically -->
            </Grid>
        </Border>
    </Grid>
</UserControl>
