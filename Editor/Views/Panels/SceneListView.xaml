<UserControl x:Class="FirstEngineEditor.Views.Panels.SceneListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="+" Width="30" Click="OnAddScene"/>
            <Button Content="-" Width="30" Click="OnRemoveScene" Margin="5,0,0,0"/>
            <Button Content="↻" Width="30" Click="OnRefresh" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Scene List -->
        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding Scenes}"
                 SelectedItem="{Binding SelectedScene}"
                 DisplayMemberPath="Name"
                 Margin="5"/>
    </Grid>
</UserControl>
